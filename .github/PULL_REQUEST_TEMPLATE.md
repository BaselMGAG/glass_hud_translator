## What this changes

<!-- One or two sentences. -->

## Why

<!-- What was wrong, or what it enables. -->

## Checked

- [ ] `dotnet build` and `dotnet test` pass
- [ ] If it touches the pipeline: `dotnet run --project tools/Replay -- --no-cache` still runs clean
- [ ] If it touches the overlay: Arabic still renders joined, and no explicit `LineHeight` was set

<!--
Adding a game profile or fixing Arabic spellings? The boxes above mostly do not apply — say so and
open it anyway. Those are the contributions this project most needs.
-->
