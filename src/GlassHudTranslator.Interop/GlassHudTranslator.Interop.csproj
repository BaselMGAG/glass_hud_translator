<Project Sdk="Microsoft.NET.Sdk">

  <!--
    P/Invoke declarations only - no logic, no state. Targets net10.0-windows so the platform
    analyzer flags any accidental call from cross-platform code, but it still compiles on macOS
    (see PROJECT_PLAN.md 1.2).
  -->
  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>GlassHudTranslator.Interop</RootNamespace>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

</Project>
